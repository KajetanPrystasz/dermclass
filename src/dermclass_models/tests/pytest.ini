[pytest]
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning

markers =
    slow: mark test as slow
    pipeline_training: mark test as one checking pipeline_training
    integration: mark test as an integration test